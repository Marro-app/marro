# Future Work

Prioritized backlog. Check off + move to UI_AUDIT_LOG when done.
(June 11 2026 overhaul cleared: light theme, self-hosted fonts, modal a11y, table edge fade, blob reduced-motion, chip states, empty-state consolidation, subs rounding, pie retitle.)

## P1
- [ ] **Conflict modal live test** — never triggered with real sync data; exercise once Gist sync is reachable (two devices or two tabs with forced divergence). Note: `darkMode` now actually diverges across devices, so a real conflict is easier to force.
- [x] **Icon-picker popover clipped behind "Key Notes" for lower categories** (June 13) — FIXED: lift the Spending-categories `Card` (`zIndex:50` while `editIconCat||iconPickOpen`), same principle as `useLiftCard`. User-confirmed working on localhost. Ships with next deploy.

## Phase 3 vision — auto term-date prediction (user request, June 14)
- [ ] **Auto-fetch academic calendar from school name + program length.** Eventually, given the school + # of years, Marro should look up that school's prior academic calendars, predict future term start/end dates, and pre-fill the year configs — editable by the user, and correctly accounting for years with an extra day (leap years / varying term lengths). The Phase 3 onboarding year-config generator is the seam for this: it currently derives dates arithmetically from a start year, but should be swappable for a calendar source later. **Design principle: do NOT overwhelm the user at setup** — keep first-run minimal, infer/predict as much as possible, let users correct afterward rather than asking up front.
  - **Approach decided (June 14): LLM extraction, lazy + cached — NOT 200 bespoke scrapers.** Writing per-school scrapers (CSS selectors over ~200 LCME+COCA sites in wildly different HTML/PDF/portal formats) is a maintenance treadmill and breaks on every redesign. Instead: (1) when a user from school X onboards with no cached calendar, run a *one-time* fetch for that school — web search → Claude reads the calendar page/PDF and extracts term dates (no selectors); (2) cache the result per-school in Supabase so every future user from X reuses it — coverage grows organically, fetch cost is once-per-school not once-per-user; (3) always editable + framed as an estimate; user corrections feed the per-user predictive tier. Same engine as the aid-letter scan below (Claude vision → structured data). Both need the small backend (server fn / Edge Function) — **build together, Phase 4.**
  - **Provider seam / 3 tiers:** the generator is a swappable date provider — tier 1 heuristic (ship now: weekday-anchored ~early-Aug start, 12-mo leap-aware spans, M3/M4 longer; budgeting only needs the financial-year boundary, not day precision), tier 2 user-corrected (extrapolate from the user's own prior real dates once entered), tier 3 fetched-calendar (seeds tier 2 from the LLM extraction above).
- [x] **Program type (MD / DO) derived from school name, never asked** (June 15) — `degreeForSchool()` regex (all COCA names contain "Osteopathic"). MD-PhD / MD-Master's / DO duals ARE asked (track step) since they can't be inferred; DO duals gated by curated `DO_DUAL` map (expandable; free-text fallback). Stored in `data.program`.
- [ ] **Financial-aid-letter / screenshot upload → auto-fill (user request, June 14).** Let the user upload a financial aid award letter (PDF or screenshot). The system scans it (Claude vision/OCR) and extracts grant/scholarship amounts, tuition, fees, health insurance, living allowance, COA, etc., then auto-fills the year config and creates appropriate spending categories. This is the low-friction alternative to manual entry — pairs with the effortless-expense-capture track. **Phase 4** (Claude advisor) territory; the year-config + categories must stay programmatically fillable so this can drop in.
- **Scalability principle (June 14):** no school gets special-cased. Every school — including Weill Cornell — flows through the same generic generated year-config (financial fields default to 0, filled later via the Aid tab or the aid-letter scan above). Do NOT hardcode any school's tuition/grant numbers as a preset.

## New backlog (user requests, June 14)
- [x] **Retire the "Re-run setup" button** (June 20) — DONE. Avatar parity confirmed (`AvatarModal` in settings, opened from the avatar button); name/school/program-track/length all editable elsewhere. Removed the button + `redoSetup` state/wiring; `OnboardingFlow` kept for first-run. Moots the redo-only "school field starts blank" quirk.
- [ ] **Dev-mode test harness (localhost-only) — NOT an auth bypass.** Testing requires a real Google login each session. Do NOT build a login-bypass/backdoor endpoint (public repo, RLS-gated; a deployed bypass = critical vuln, account impersonation). Instead: a localhost-only flag that loads the app with mock session + fake sample data, never deployed, never touching the real account/Supabase. Lets automated UI testing run without credentials. (Current safe workflow: user logs in once at session start; the browser session token persists and is reused.)

## New backlog (user requests, June 13)
- [ ] **Effortless expense capture (the make-or-break)** — CSV import is too much friction; nobody will do it. Two tracks: (a) **Voice / natural-language entry** — "I got a Starbucks latte today" → Claude parses merchant/amount/category → pre-fills a weekly entry for one-tap confirm. Fits **Phase 4** (Claude advisor); start here, higher leverage + cheaper. (b) **Bank connection (Plaid or similar)** — auto-import transactions. Heavier: recurring cost, OAuth/security, dedup against manual entries. Defer behind (a).
- [x] **Hover affordance pass** (June 20) — DONE. Primary target = the **nav pills** (tab bar + year selector): inactive `TabBtn`/`YrBtn` now carry a `.navpill` class whose `:hover` brightens the lettering (`--text`, via `!important` over the inline muted color) + adds a faint `--input-bg` wash; active pills stay unclassed so their highlight is untouched. Verified in-browser (hovering "Weekly" / "Year 3" brightens + washes). Also added a shared `.txt-act` class (opacity 0.72 → 1 + cursor) on the grayish secondary text actions: onboarding "← Back" (×4) and "Change" buttons. `.xbtn` ghost-✕ hover already existed; teal links already signal via color. Still open if wanted: an actual `.mc-sh`-style sheen sweep across the lettering (current effect is brighten+wash, not a moving shimmer).

## Admin backend (invites + analytics) — needs a server-side component
Both items below require the **Supabase service-role key behind a server function** (small Vercel serverless fn or Supabase Edge Function — the public client can't be trusted to read all users or manage an allowlist). This reintroduces a tiny backend (different from the deleted `api/sync.js`). Gated on the OAuth verification item below.
- [ ] **Invite / auto-add users** — NOT via Google's "test users" list (console-manual, 100 cap, no clean API — dead end). Instead: publish+verify the consent screen so any Google account *can* sign in, then gate access app-side with a Supabase `allowed_emails` (invite) table. Admin panel "Invite" = insert email → on that user's first Google login the app calls `is_email_allowed` RPC → grant or sign-out+deny. Google does identity; Supabase does gating. **SQL written & ready: `supabase/allowed_emails.sql`** (table + RLS + `is_email_allowed()` SECURITY DEFINER fn, owner seeded). NOT yet run/enforced — do not enable the app-side gate until the consent screen leaves Testing mode (lockout risk; Google test-user list already gates now). App-side hook documented in §4 of that file.
- [ ] **Legal pages** — `privacy.html` + `terms.html` drafted (June 13), on-brand, live at `/privacy.html` `/terms.html` once deployed. DRAFT quality — have a human review before submitting for Google verification; swap contact email if a dedicated support address is created; confirm NY governing law.
- [ ] **Admin analytics panel** — who signed up, login frequency, last-seen, feature usage. Raw data is largely free in `auth.users` (`last_sign_in_at`, `created_at`); richer usage needs lightweight event logging. Read via the Admin API behind the same server function. Same backend as invites — build together.

## P2 — quality
- [x] **apple-touch-icon is an SVG** (June 20) — DONE. Exported a 180×180 PNG of the rings icon (`apple-touch-icon.png`, opaque #14150F bg) and pointed `<link rel="apple-touch-icon">` at it.
- [x] Dual `<meta name="theme-color">` with `media="(prefers-color-scheme)"` (June 20) — DONE. Two media-scoped metas (light #ECEAE2 / dark #101210) for pre-JS chrome; `applyTheme` now sets `content` on *all* theme-color metas so whichever the browser picks by OS pref shows the user's actual chosen theme. (Still open: revisit `apple-mobile-web-app-status-bar-style` for light theme.)
- [x] Light-theme `.mc-sh` shimmer (June 20) — DONE. Added `[data-theme="light"] .mc-sh` override (white 0.10 → 0.45) so the corner sheen is visible on light surfaces. `.mc-sp` specular hairline left as-is (still faint white-on-white; harmless — revisit only if it bothers).
- [x] Sweep hardcoded cream rgba stragglers (June 20) — DONE. Week-pill "← Back to current week" border/fill (was `rgba(246,239,221,0.45)` / `.10`) → theme tokens `C.sel` / `C.selBg`. MarroLogo tile shadow already theme-aware (light hairline + lift). No other JSX cream stragglers found via grep.

## P3 — opportunities (incl. deferred from Phase 2.5)
- [ ] Tab bar redesign: floating glass pill detached from content, cross-fade tab transitions.
- [ ] Charts: gradient fills + draw-on animation, themed `cursor` fills audit.
- [ ] Number-roll animation on MetricTile values.
- [ ] CSV import: remember per-merchant category corrections (extend `autoCategory` with learned map in state).
- [ ] Browse-weeks modal: show entry count next to each archived week's total.
- [ ] Bloom state currently triggers only from "Log deposit" — also trigger when a weekly exams entry overflows a goal to fully funded.

## Pre-public-launch (Phase 2.5b → before non-WCM testers)
- [ ] **Google OAuth consent shows raw Supabase domain** ("Sign in to <ref>.supabase.co") + "unverified app" warning. Harmless in testing, looks sketchy to new users. Fix at launch: (1) buy a domain, (2) add it as a Supabase **custom auth domain** (paid add-on, needs Pro plan) so the callback is on our domain, (3) add a real app logo, (4) submit the Google consent screen for **verification** (removes the warning + shows "Marro" + logo). Pairs with publishing the consent screen out of Testing mode (currently capped at 100 test users).
